_schema-version: "3.1"
ID: ccap-products
description: A simple CAP project.
version: 1.0.0
modules:
- name: ccap-products-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: ccap-products-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: ccap-products-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: ccap-products-db
  parameters:
    buildpack: nodejs_buildpack
- name: ccap-products-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ccap-products-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - logaligroupproducts.zip
      name: logaligroupproducts
      target-path: resources/
- name: logaligroupproducts
  type: html5
  path: app/products
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ccap-products-db
  type: com.sap.xs.hdi-container
  parameters:
    config:
      schema: CCAP
    service: hana
    service-plan: hdi-shared
- name: ccap-products-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ccap-products-html5-srv
    service-plan: app-host
- name: ccap-products-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ccap-products-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
